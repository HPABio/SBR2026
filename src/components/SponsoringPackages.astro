---
import SponsoringPackagesComponent from "@/components/ui/sponsoring-packages.tsx";

interface Props {
  class?: string;
}

const { class: classProp } = Astro.props;
---

<SponsoringPackagesComponent client:load className={classProp} />
