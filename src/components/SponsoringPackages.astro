---
import SponsoringPackagesComponent from "@/components/ui/sponsoring-packages.tsx";

interface Props {
  class?: string;
  hideGate?: boolean;
}

const { class: classProp, hideGate = false } = Astro.props;
---

<SponsoringPackagesComponent client:load className={classProp} hideGate={hideGate} />
